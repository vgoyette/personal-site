- content_for :title, "Vincent"

div class="relative isolate min-h-dvh overflow-hidden bg-canvas text-ink"
  = render Home::AtmosphereComponent.new
  = render Site::NavComponent.new(current: "home")
  = render Home::HeroComponent.new( \
      brand: "Vincent", \
      headline: "Building systems that remember why.", \
      lede: "A thin personal site for work, notes, and experiments — kept light on purpose.", \
      primary_cta: { href: posts_path, label: "Read the writing" }, \
      secondary_cta: { href: projects_path, label: "See the projects" })
  #footer
    = render Site::FooterComponent.new(email: "vgoyette22@gmail.com")
