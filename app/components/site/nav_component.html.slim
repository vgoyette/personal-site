nav class="relative z-20 border-b border-line/60 bg-canvas/70 backdrop-blur-md"
  div class="mx-auto flex w-full max-w-5xl items-center justify-between px-6 py-5 sm:px-10 lg:px-16"
    = link_to root_path, class: "font-mono text-xs uppercase tracking-[0.28em] text-ink transition duration-200 hover:text-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent"
      = brand
    div class="flex items-center gap-6"
      - links.each do |link|
        = link_to link[:label], link[:href], class: link_classes(link[:key])
